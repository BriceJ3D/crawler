{% extends 'base.html.twig' %}

{% block title %}User index{% endblock %}

{% block body %}
    <div class="recherche">
    	<h1>Nouvelle recherche</h1>
       
        <button class="btn btn-secondary btn-sm" type="button" id="addCity">Ajouter les villes</button> 
        <button class="btn btn-secondary btn-sm" type="button" id="generateUrl">Générer url</button> 
        {{ form_start(form) }}
		{{ form_widget(form) }}
		{{ form_end(form) }}
	</div>
	
{% endblock %}
      